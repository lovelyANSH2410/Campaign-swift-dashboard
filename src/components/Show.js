import React from "react";
import { leaf_bg } from "../utils/constants";

const Show = () => {
  return (
    <div className="">
      <img
      class="w-[25%] opacity-30 mx-auto mt-96"
        src={leaf_bg}
        alt="logo"
      />
    </div>
  );
};

export default Show;
